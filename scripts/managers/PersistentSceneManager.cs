using Godot;
using System;

// This should contain a dictionary that takes in the a dictionary of scene facts and game state, and outputs an array of game objects to be spawned
public partial class PersistentSceneManager : Node
{
	// Called when the node enters the scene tree for the first time.
	public override void _Ready()
	{
	}

	// Called every frame. 'delta' is the elapsed time since the previous frame.
	public override void _Process(double delta)
	{
	}
}
